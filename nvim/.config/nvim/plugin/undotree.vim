" Set the window layout (2 is undo tree on left, diff covering the entire bottom)
let g:undotree_WindowLayout = 2

" Height of the diff window
let g:undotree_DiffpanelHeight = 12

" Give focus to undotree window
let g:undotree_SetFocusWhenToggle = 1

" Hide the label \"Press ? for help\"
let g:undotree_HelpLine = 0
